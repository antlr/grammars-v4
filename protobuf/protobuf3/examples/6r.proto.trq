(: Check whether field with TestEnum is parsed as enum type. :)

if (count(//type_/enumType/enumName/ident/IDENTIFIER[text() = "TestEnum"]) = 1)
then ""
else "Field TestEnum is not being parsed correctly."
