expression
 vectorOperation
  vectorOperation
   vector
    instantSelector
     container_cpu_usage_seconds_total
  multOp
   *
   grouping
    on_
     on
     labelNameList
      (
      labelName
       pod
      )
  vectorOperation
   vector
    instantSelector
     kube_pod_info
 <EOF>
