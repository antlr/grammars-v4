expression
 vectorOperation
  vectorOperation
   vector
    function_
     rate
     (
     parameter
      vectorOperation
       vector
        matrixSelector
         instantSelector
          container_cpu_usage_seconds_total
         [1m]
     )
  multOp
   /
   grouping
    on_
     on
     labelNameList
      (
      labelName
       instance
      ,
      labelName
       job
      )
    groupLeft
     group_left
  vectorOperation
   vector
    instantSelector
     test_num_cpus
 <EOF>
