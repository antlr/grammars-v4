expression
 vectorOperation
  vectorOperation
   vector
    instantSelector
     container_cpu_usage_seconds_total
  multOp
   *
   grouping
    on_
     on
     labelNameList
      (
      labelName
       pod
      )
    groupLeft
     group_left
     labelNameList
      (
      labelName
       node
      )
  vectorOperation
   vector
    instantSelector
     kube_pod_info
 <EOF>
