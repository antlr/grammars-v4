expression
 vectorOperation
  vector
   aggregation
    max
    by
     by
     labelNameList
      (
      labelName
       some_entry
      ,
      labelName
       another_one
      )
    parameterList
     (
     parameter
      vectorOperation
       vector
        function_
         max_over_time
         (
         parameter
          vectorOperation
           vector
            instantSelector
             a_metric
             {
             labelMatcherList
              labelMatcher
               labelName
                important
               labelMatcherOperator
                =
               "thing"
             }
         )
     )
 <EOF>
