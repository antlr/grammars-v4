expression
 vectorOperation
  vector
   aggregation
    count_values
    by
     by
     labelNameList
      (
      labelName
       node
      )
    parameterList
     (
     parameter
      literal
       'pod'
     ,
     parameter
      vectorOperation
       vector
        instantSelector
         container_cpu_usage_seconds_total
     )
 <EOF>
